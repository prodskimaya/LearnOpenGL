# Learn OpenGL
My repo for learning opengl. Uses the Waf build system.

## Building
Glfw must be installed on your system via package manager in order to build. Sorry Windows Users, I'll fix it eventually.
```
python waf configure build
```
